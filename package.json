{
  "name": "lighting-ocr-v3-4-12",
  "version": "3.4.12",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp sample_data.csv dist/sample_data.csv"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}